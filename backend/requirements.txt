# Duplicado em requirements.txt na raiz do repo pro build da Vercel (o
# parser dela não aceita "-r" incluindo este arquivo — ver comentário lá).
# Ao mudar algo aqui, atualize o outro arquivo também.
flask>=3.0
flask-cors>=4.0
pyjwt>=2.8
werkzeug>=3.0
python-dotenv>=1.0
psycopg[binary]>=3.2
psycopg_pool>=3.2
requests>=2.31
openai>=1.40
pytest>=8.0
pytest-cov>=5.0
