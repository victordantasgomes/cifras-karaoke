# Dev local: aponta pro Flask rodando à parte (também dá pra deixar em
# branco e usar o proxy /api -> :5000 já configurado em vite.config.js).
# Produção (Vercel): NÃO defina essa variável — front e back ficam no mesmo
# domínio/projeto, então api.js já cai em baseURL '/api' (mesma origem).
VITE_API_URL=http://localhost:5000
